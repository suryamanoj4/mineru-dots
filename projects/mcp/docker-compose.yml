version: '3'

services:
  vparse-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      - VPARSE_API_KEY=${VPARSE_API_KEY}
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped 